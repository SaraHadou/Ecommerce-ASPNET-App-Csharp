@model int

@if (Model > 0)
{
    <div class="nav-item">
        <a class="text-dark nav-link" asp-controller="Orders" asp-action="ShoppingCart">
            <i class="bi bi-cart4"></i> @Model
        </a>
    </div>
}